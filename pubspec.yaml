name: cbj_smart_device_flutter
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.1+2

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cbj_smart_device:
    git: https://github.com/guyluz11/cbj_smart_device.git
  cupertino_icons: ^1.0.6
  image_picker: ^1.0.4
  camera: ^0.10.5+5
  wakelock: ^0.6.2


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
